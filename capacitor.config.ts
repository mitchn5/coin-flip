import type { CapacitorConfig } from '@capacitor/cli';

//Amaya is the best partner in the world, I love her so much <3
const config: CapacitorConfig = {
  appId: 'com.mnorris.coinflip',
  appName: 'Flipping',
  webDir: 'build'
};

export default config;
